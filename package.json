{
    "name": "delete-test-01",
    "description": "delete-test-01-description",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.799.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.51",
        "@types/node": "^13.13.5",
        "jest": "^26.6.3",
        "ts-node": "^9.1.1",
        "typescript": "^3.8.3"
    },
    "scripts": {
        "compile": "tsc",
        "deploy": "npm run compile && sam deploy --guided",
        "test": "jest --roots __tests__/unit",
        "integ-test": "jest --roots __tests__/integration"
    }
}
